#include "meter_dcv.h"

QState meter_dcv_adc(ao_meter_t *const me)
{
    QState status;

    status = Q_HANDLED();

    return status;
}

QState meter_dcv_key(ao_meter_t *const me)
{
    QState status;

    status = Q_HANDLED();

    return status;
}
